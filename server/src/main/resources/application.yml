server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/support_portal
    username: root
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect